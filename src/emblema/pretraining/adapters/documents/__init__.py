"""The documents Pretraining stores and reads back: configurations, orders and results as JSON.

Written by this context for this context on another machine, so they are not a published
language; they are stored, which is why they have a form that outlives a process.
"""
