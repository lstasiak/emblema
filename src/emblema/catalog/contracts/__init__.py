"""Published language of the Data Catalog: the only Catalog package other contexts may import.

Identifiers are vocabulary shared with the Catalog's own domain. References and events are
messages built by its application layer from frozen versions; they depend on ``emblema.shared``
alone.
"""
